/**
 * MSW request handlers for mocking API requests in tests.
 * Add your API mock handlers here.
 *
 * Example:
 * import { http, HttpResponse } from "msw";
 *
 * export const handlers = [
 *   http.get('/api/books', () => {
 *     return HttpResponse.json([{ id: 1, title: 'Test Book' }]);
 *   }),
 * ];
 */
export const handlers = [];
